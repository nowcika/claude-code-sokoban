{
  "name": "claude-code-sokoban-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright E2E tests for Claude Code Sokoban (canvas-based educational puzzle)",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
